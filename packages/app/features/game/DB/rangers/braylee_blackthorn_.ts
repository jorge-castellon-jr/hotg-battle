import { BaseRangerCard, RangerCard } from '../../Card/CardTypes';
import { createDeck } from '../cardUtils';

const deck: RangerCard[] = createDeck(
  [],
  {
    team: 'Infinity Force',
    owner: 'braylee_blackthorn_',
    color: 'yellow'
  }
);

export default deck;